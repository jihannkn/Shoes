/* eslint-disable react/prop-types */
export default function AsideLink({ children }) {
    return (
        <div className="choose flex flex-col items-center gap-1 text-[12px]">
            {children}
        </div>
    )
}